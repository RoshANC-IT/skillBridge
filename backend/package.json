{
  "name": "skill-link",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "check-env": "node -e \"require('dotenv').config(); const required = ['MONGODB_URL', 'JWT_SECRET']; const missing = required.filter(r => !process.env[r]); if (missing.length) { console.error('Missing:', missing.join(', ')); process.exit(1); } else { console.log('✅ All required env vars present'); }\""
  },
  "author": "Rohit",
  "license": "ISC",
  "type": "module",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3",
    "multer": "^2.0.2",
    "nodemon": "^3.1.10",
    "otp-generator": "^4.0.1",
    "socket.io": "^4.8.1"
  }
}
